import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { getConditionerEntitlement } from '@/lib/commerce/entitlement'
import { AssessmentForm } from '@/components/assessment-form'
export default async function Page(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');if(!await getConditionerEntitlement(user.id))redirect('/my-blend-bar');return <div className="shell max-w-4xl py-12"><div className="eyebrow">Hair Need Assessment · V1</div><h1 className="display mt-3 text-4xl sm:text-5xl">Start with what your hair is doing now.</h1><p className="mt-4 max-w-3xl leading-7 text-[#203e33]/65">This assessment separates your underlying hair characteristics from its current condition. It does not assign a traditional hair type, and it does not use ethnicity as a proxy for need.</p><div className="mt-4 rounded-2xl border border-[#203e33]/10 bg-[#e8ddc9]/55 p-4 text-sm leading-6">Choose the answer that best reflects your recent experience. Your result is educational guidance for the workshop, not a medical diagnosis.</div><div className="mt-8"><AssessmentForm/></div></div>}
